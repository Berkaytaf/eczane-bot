name: 🚀 Lead Gen Master (Eczane & İşletme Avcısı)

on:
  workflow_dispatch:
    inputs:
      sehir:
        description: 'Hangi Şehir? (Örn: Istanbul)'
        required: true
        default: 'Istanbul'
      kategori:
        description: 'Ne Arıyoruz? (Örn: Eczane, Restoran, HVAC)'
        required: true
        default: 'Eczane'

jobs:
  scrape_job:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      
    steps:
      - name: 📂 Kodları Çek
        uses: actions/checkout@v4

      - name: 🐍 Python Kur
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: 📦 Kütüphaneleri ve Tarayıcıyı Yükle
        run: |
          python -m pip install --upgrade pip
          pip install playwright pandas openpyxl
          # Linux sunucusunda Playwright'ın çalışması için sistem kütüphanelerini de kuruyoruz:
          playwright install --with-deps chromium

      - name: 🤖 Botu Ateşle
        # Girdileri tırnak içinde gönderiyoruz ki boşluklu isimlerde patlamasın
        run: python pro_scraper.py "${{ github.event.inputs.sehir }}" "${{ github.event.inputs.kategori }}"

      - name: 📥 Excel Dosyasını Hazırla (Artifact)
        # Bot bittiğinde Excel'i sağ üstteki 'Summary' kısmından indirmeni sağlar
        uses: actions/upload-artifact@v4
        with:
          name: ${{ github.event.inputs.sehir }}-${{ github.event.inputs.kategori }}-Listesi
          path: "*.xlsx"
          if-no-files-found: error

      - name: 📊 Sonucu Repoya Kaydet
        # Dosyayı repoda da görmek istersen burası çalışır
        run: |
          git config --global user.name "LeadGenBot"
          git config --global user.email "bot@github.com"
          git add *.xlsx || echo "Yeni dosya yok"
          git commit -m "✅ Veri Güncellendi: ${{ github.event.inputs.sehir }}" || echo "Değişiklik yok"
          git push || echo "Push yapılamadı"
