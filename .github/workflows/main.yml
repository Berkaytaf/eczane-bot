name: 🚀 Lead Gen Master (Multi-Source & Dashboard)

on:
  workflow_dispatch:
    inputs:
      sehir:
        description: 'Hangi Şehir? (Örn: Istanbul)'
        required: true
        default: 'Istanbul'
      kategori:
        description: 'Ne Arıyoruz? (Örn: Eczane, Restoran, HVAC)'
        required: true
        default: 'Eczane'
  schedule:
    - cron: '0 0 * * *' # Her gece 00:00'da otomatik çalışır

jobs:
  scrape_job:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      
    steps:
      - name: 📂 Kodları Çek
        uses: actions/checkout@v4

      - name: 🐍 Python Kur
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: 📦 Kütüphaneleri ve Tarayıcıyı Yükle
        run: |
          python -m pip install --upgrade pip
          pip install playwright pandas openpyxl
          playwright install --with-deps chromium

      - name: 🤖 Botu Ateşle
        run: python pro_scraper.py "${{ github.event.inputs.sehir || 'Istanbul' }}" "${{ github.event.inputs.kategori || 'Eczane' }}"

      - name: 📊 Dashboard & Veriyi Repoya Kaydet
        run: |
          git config --global user.name "LeadGenBot"
          git config --global user.email "bot@github.com"
          git add database.json index.html leads_output.xlsx || echo "Dosya yok"
          git commit -m "✅ Veri İndeksi Güncellendi: $(date)" || echo "Değişiklik yok"
          git push || echo "Push yapılamadı"

      - name: 📥 Excel Dosyasını Hazırla (Artifact)
        uses: actions/upload-artifact@v4
        with:
          name: Lead-Gen-Export
          path: leads_output.xlsx
